namespace ShapeIntersectionEngine.InputParsing
{
    /// <summary>
    /// Represents known types of shapes. Used to tell the parser what kind of shape to construct
    /// </summary>
    public enum ValidShapes
    {
        Cube,
        Box,
        Sphere
    }
}
